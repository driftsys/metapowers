# Adding two numbers

We built `add` because summing values is a common need, and we chose keyword-only
options for forward compatibility — a design decision worth understanding before
you use it.

To add two numbers, call `add(a, b)` and it returns the sum.

Click [here](#) for the rounding options.
