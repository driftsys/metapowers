# Legacy import brief

Staged directly under `docs/wip/` by a hypothetical import/migration skill
(modeled on SEED's `seed-import`), outside the Superpowers spec/plan pair
this sandbox's `docs/wip/specs/` and `docs/wip/plans/` represent. Not a
spec, not a plan — the gardener must not attempt to reconcile this into the
`docs/` record taxonomy, and must not delete it.
